# LLM Profiles Configuration - REPAIRED FOR CPU ONLY
llm_profiles:
  # Fast, cheap models for bulk operations
  cheap_bulk:
    candidates:
      - qwen2.5:3b
      - llama3:8b
    max_cost_per_1k: 0.0
    description: "Local models for bulk processing"

  # Default profile
  default:
    candidates:
      - qwen2.5:3b
      - llama3:8b
    max_cost_per_1k: 0.0
    description: "Default profile using local Ollama"

  # Laptop profile - CPU Optimized
  laptop:
    candidates:
      - phi3:latest
      - deepseek-coder:1.3b
    max_cost_per_1k: 0.0
    description: "Resource-efficient profile for laptop usage"
