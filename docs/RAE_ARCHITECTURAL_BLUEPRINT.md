# 🏛️ RAE Architectural Blueprint: Silicon Oracle V4.16
> **A Comprehensive Guide to Autonomous Memory, Hierarchical Cognition, and Fluid Mathematics.**

This document serves as the definitive reference for the RAE (Reflective Agentic Engine) ecosystem. It maps the internal reality of the code from data ingestion to complex neural reasoning.

---

## 🛠️ I. The 5 Layers of Silence (Data Foundation)
RAE manages data across five distinct layers of persistence and abstraction, ensuring both security (ISO 27001) and long-term utility.

1.  **Working Layer (`working`)**: 
    - *Role:* Operational short-term context.
    - *Security:* Strict `RESTRICTED` isolation. Data is encrypted and volatile.
2.  **Episodic Layer (`episodic`)**: 
    - *Role:* Event logging and A2A interactions.
    - *Capture:* 100% Implicit Capture via the Intelligent Bridge.
3.  **Semantic Layer (`longterm`)**: 
    - *Role:* Fact storage, rules, and coding standards (e.g., PSR, Project Wiki).
    - *Structure:* Linked to the Knowledge Graph.
4.  **Reflective Layer (`reflective`)**: 
    - *Role:* Synthetic knowledge generated by the Reflection Engines.
    - *Output:* High-level abstractions and "lessons learned".
5.  **LTM Layer (`ltm`)**: 
    - *Role:* Historical archive of critical knowledge surviving the decay process.

---

## 🧠 II. The 4 Levels of Cognition (Reflection Hierarchy)
Unlike traditional RAG systems, RAE performs continuous self-audit through four recursive reflection layers.

| Layer | Component | Function |
| :--- | :--- | :--- |
| **L1** | **Operational** | Verifies evidence grounding and enforces contract compliance. |
| **L2** | **Structural** | Analyzes graph topology and optimizes token/API costs. |
| **L3** | **Meta (Szubar)** | Identifies knowledge gaps ("MISS" detection) and repairs search errors. |
| **L4** | **Cognitive** | Global knowledge synthesis and project-wide pattern detection. |

---

## 🧮 III. The 3 Math Engines (Reasoning Pipeline)
Retrieval in RAE is a hierarchical process executed by the **Logic Gateway**.

### Engine 1: Heuristic (Fast Path)
- **Algorithms:** BM25, Regex, Anchor Matching.
- **Latency:** <1ms.
- **Goal:** Deterministic precision for specific symbols and IDs.

### Engine 2: Probabilistic (Resonance)
- **Algorithms:** Semantic Resonance, Graph Induction.
- **Logic:** Propagates "energy" through neighbors in the Knowledge Graph to find related context.
- **Outcome:** Linguistic sharpening and discovery of non-obvious links.

### Engine 3: Neural (Deep Path)
- **Algorithms:** ONNX Cross-Encoders (Neural Scalpel).
- **Strategy:** Full re-ranking of top 300 candidates.
- **Trigger:** Activated by the **Policy Router** when Fast Path confidence < 0.85.

---

## 📜 IV. The Manifold Atlas (Strategy Registry)
RAE can emulate hundreds of historical mathematical models. These are registered as **Manifold Arms** and selected by the **Multi-Armed Bandit (MAB)** controller.

| Arm ID | Historical Name | Key Characteristic |
| :--- | :--- | :--- |
| `classic_ib` | System 1.0 | Information Bottleneck (Max compression). |
| `hyper_37` | System 37.0 | Exponential Rank Sharpening (100k scaling). |
| `fluid_100` | System 100.0 | Dynamic Geometry scaling via system entropy. |
| `neural_22` | System 22.1 | Neural-first re-ranking (10,000x weight). |
| `doc_pro` | System 41.4 | Tier-based proof factor for technical docs. |

---

## 🔐 V. The Security & Governance Contract
All operations are governed by the **Enterprise Guard**, ensuring:
- **Auditability:** `human_label` assigned to every memory.
- **Traceability:** Automatic measurement of `duration_s` and `mem_delta_mb`.
- **Zero-Egress:** native execution of LLM logic via ONNX where possible.

---
*Blueprint Version 1.0 - Generated by Gemini-RAE Orchestrator.*
