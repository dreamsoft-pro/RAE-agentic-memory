#!/bin/sh

envsubst < /etc/php/8.1/fpm/pool.d/www.conf.tmp  > /etc/php/8.1/fpm/pool.d/www.conf
envsubst '$NGINX_REAL_IP,$NGINX_GZIP,$NGINX_EXTRAS' < /etc/nginx/app.conf.tmp > /etc/nginx/sites-enabled/app.conf

[ -f /usr/bin/entrypoint.sh ] && /usr/bin/entrypoint.sh


exec $@
