import React from "react";

export const LayerDownButton = ({ layerFunction }) => {
    return (
        <div className={`position-button layer-down-button`} onClick={layerFunction}> </div>
    );
};

