export const INCREMENT_ANGLE = 'INCREMENT_ANGLE';
export const DECREMENT_ANGLE = 'DECREMENT_ANGLE';

export const incrementAngle = () => ({
    type: INCREMENT_ANGLE
});

export const decrementAngle = () => ({
    type: DECREMENT_ANGLE
});
