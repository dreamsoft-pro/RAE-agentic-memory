name: "Industrial Oracle"
description: "Optimized for machine logs and structured industrial data"
strategy:
  fusion: "reciprocal_rank"
  weights:
    dense: 0.2
    sparse: 2.0
    fulltext: 20.0
  thresholds:
    min_similarity: 0.95
    scalpel_mode: true
  features:
    use_neural_scalpel: true
    metadata_injection: true
