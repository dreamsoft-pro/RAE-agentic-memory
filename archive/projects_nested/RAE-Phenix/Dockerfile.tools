# Użyj oficjalnego obrazu z nową wersją Node.js
FROM node:20

# Ustaw katalog roboczy wewnątrz kontenera
WORKDIR /usr/src/app

# Skopiuj pliki package.json i package-lock.json
COPY package*.json ./

# Zainstaluj zależności zdefiniowane w package.json dla tego narzędzia
RUN npm install

# Skopiuj resztę plików (twój skrypt do indeksacji i pliki do analizy)
COPY . .

# Komenda, która zostanie wykonana po uruchomieniu kontenera
# Załóżmy, że twój skrypt nazywa się js_html_indexer.mjs
CMD [ "node", "js_html_indexer.mjs" ]