import React from 'react'

const ImageToPageButton = ({...props}) => {
    return (
        <button
            {...props}
            className={'bitmap-tool-button image-to-page'}>

        </button>
    )
}

export default ImageToPageButton