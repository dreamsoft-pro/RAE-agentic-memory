import React from 'react'

const ImageToCenterButton = ({...props}) => {
    return (
        <button
            {...props}
            className={'bitmap-tool-button image-to-center'}>

        </button>
    )
}

export default ImageToCenterButton